VITE_API_BASE_URL=http://localhost:4000

# 정적 베타 배포에서 명시적으로 API 호출을 끌 때 사용합니다.
# Cloudflare Pages / Vercel은 빌드 환경을 자동 감지하지만, 필요하면 아래 값을 배포 환경변수로 추가하세요.
# VITE_DEPLOY_TARGET=static

# GitHub Pages에서 새로고침 404를 피하려면 HashRouter를 사용하세요.
# VITE_ROUTER_MODE=hash
# VITE_BASE_PATH=/a-blog-allinone/
